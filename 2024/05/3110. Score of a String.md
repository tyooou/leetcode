---
link: https://leetcode.com/problems/score-of-a-string/description/
difficulty: Easy
date: 2024-05-23
tags: [string]
runtime: "1 ms"
memory: "41.9 MB"
---

### Problem Statement:

You are given a string `s`. The **score** of a string is defined as the sum of the absolute difference between the **ASCII** values of adjacent characters.

Return the **score** of_ _`s`.

> [!example]- Example 1
> **Input:** s = "hello"
>
> **Output:** 13
>
> **Explanation:**
>
> The **ASCII** values of the characters in `s` are: `'h' = 104`, `'e' = 101`, `'l' = 108`, `'o' = 111`. So, the score of `s` would be `|104 - 101| + |101 - 108| + |108 - 108| + |108 - 111| = 3 + 7 + 0 + 3 = 13`.

> [!example]- Example 2
> **Input:** s = "zaz"
>
> **Output:** 50
>
> **Explanation:**
>
> The **ASCII** values of the characters in `s` are: `'z' = 122`, `'a' = 97`. So, the score of `s` would be `|122 - 97| + |97 - 122| = 25 + 25 = 50`.

> [!warning]- Constraints
> - `2 <= s.length <= 100`
>
> 	- `s` consists only of lowercase English letters.

---

### Hints:

> [!success]- Hint 1
> Sum the difference between all the adjacent characters by just taking the absolute difference of their ASCII values.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `1 ms` | `41.9 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```java
>
>
> class Solution {
>     public int scoreOfString(String s) {
>         char[] split = s.toCharArray();
>         int sum = 0;
>         for (int i = 1; i < split.length; i++) {
>             int abs1 = (int) split[i - 1];
>             int abs2 = (int) split[i];
>             sum += Math.abs(abs1 - abs2);
>         }
>         return sum;
>     }
> }
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
