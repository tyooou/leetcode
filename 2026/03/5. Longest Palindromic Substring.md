---
link: https://leetcode.com/problems/longest-palindromic-substring/description/
difficulty: Medium
date: 2026-03-07
tags: [two-pointers, string, dynamic-programming, manacher]
runtime: "1198 ms"
memory: "26.8 MB"
---

### Problem Statement:

Given a string `s`, return _the longest_ _palindromic_ _substring_ in `s`.

> [!example]- Example 1
> **Input:**
>
> ```python
> s = "babad"
> ```
>
> **Output:**
>
> ```python
> "bab"
> ```
>
> **Explanation:**
>
> ```python
> "aba" is also a valid answer.
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> s = "cbbd"
> ```
>
> **Output:**
>
> ```python
> "bb"
> ```

> [!warning]- Constraints
> - `1 <= s.length <= 1000`
>
> 	- `s` consist of only digits and English letters.

---

### Hints:

> [!success]- Hint 1
> How can we reuse a previously computed palindrome to compute a larger palindrome?

> [!success]- Hint 2
> If “aba” is a palindrome, is “xabax” a palindrome? Similarly is “xabay” a palindrome?

> [!success]- Hint 3
> Complexity based hint:
> If we use brute-force and check whether for every start and end position a substring is a palindrome we have O(n^2) start - end pairs and O(n) palindromic checks. Can we reduce the time for palindromic checks to O(1) by reusing some previous computation.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `1198 ms` | `26.8 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def longestPalindrome(self, s: str) -> str:
>         sLen = len(s)
>
>         # r and c are slice indices of string s
>         dpMatrix = [[False] * len(s) for _ in range(len(s))]
>
>         # every single character is a palindrome
>         for i in range(sLen):
>             dpMatrix[i][i] = True
>
>         res = s[0] # default to a single char
>
>         for j in range(sLen):
>             for i in range(j):
>                 if s[i] == s[j] and (dpMatrix[i+1][j-1] or j == i + 1):
>                     dpMatrix[i][j] = True
>                     if (j - i + 1) > len(res):
>                         res = s[i:j+1]
>
>         return res
>
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
