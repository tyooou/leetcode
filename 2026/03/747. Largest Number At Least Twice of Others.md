---
link: https://leetcode.com/problems/largest-number-at-least-twice-of-others/description/
difficulty: Easy
date: 2026-03-21
tags: [array, sorting]
runtime: "0 ms"
memory: "19.3 MB"
---

### Problem Statement:

You are given an integer array `nums` where the largest integer is **unique**.

Determine whether the largest element in the array is **at least twice** as much as every other number in the array. If it is, return _the **index** of the largest element, or return _`-1`_ otherwise_.

> [!example]- Example 1
> **Input:**
>
> ```python
> nums = [3,6,1,0]
> ```
>
> **Output:**
>
> ```python
> 1
> ```
>
> **Explanation:**
>
> ```python
> 6 is the largest integer.
> For every other number in the array x, 6 is at least twice as big as x.
> The index of value 6 is 1, so we return 1.
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> nums = [1,2,3,4]
> ```
>
> **Output:**
>
> ```python
> -1
> ```
>
> **Explanation:**
>
> ```python
> 4 is less than twice the value of 3, so we return -1.
> ```

> [!warning]- Constraints
> - `2 <= nums.length <= 50`
>
> 	- `0 <= nums[i] <= 100`
>
> 	- The largest element in `nums` is unique.

---

### Hints:

> [!success]- Hint 1
> Scan through the array to find the unique largest element `m`, keeping track of it's index `maxIndex`.
>
> Scan through the array again.  If we find some `x != m` with `m < 2*x`, we should return `-1`.
>
> Otherwise, we should return `maxIndex`.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `0 ms` | `19.3 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def dominantIndex(self, nums: List[int]) -> int:
>         sNums = sorted(nums)
>         largest = sNums[-1]
>         res = (largest // 2 >= sNums[-2])
>
>         return nums.index(largest) if res else -1
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
