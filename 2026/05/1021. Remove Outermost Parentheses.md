---
link: https://leetcode.com/problems/remove-outermost-parentheses/description/
difficulty: Easy
date: 2026-05-07
tags: [string, stack, bracket-sequences]
runtime: "4 ms"
memory: "19.4 MB"
---

### Problem Statement:

A valid parentheses string is either empty `""`, `"(" + A + ")"`, or `A + B`, where `A` and `B` are valid parentheses strings, and `+` represents string concatenation.

	- For example, `""`, `"()"`, `"(())()"`, and `"(()(()))"` are all valid parentheses strings.

A valid parentheses string `s` is primitive if it is nonempty, and there does not exist a way to split it into `s = A + B`, with `A` and `B` nonempty valid parentheses strings.

Given a valid parentheses string `s`, consider its primitive decomposition: `s = P_1 + P_2 + ... + P_k`, where `P_i` are primitive valid parentheses strings.

Return `s` _after removing the outermost parentheses of every primitive string in the primitive decomposition of _`s`.

> [!example]- Example 1
> **Input:**
>
> ```python
> s = "(()())(())"
> ```
>
> **Output:**
>
> ```python
> "()()()"
> ```
>
> **Explanation:**
>
> ```python
> The input string is "(()())(())", with primitive decomposition "(()())" + "(())".
> After removing outer parentheses of each part, this is "()()" + "()" = "()()()".
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> s = "(()())(())(()(()))"
> ```
>
> **Output:**
>
> ```python
> "()()()()(())"
> ```
>
> **Explanation:**
>
> ```python
> The input string is "(()())(())(()(()))", with primitive decomposition "(()())" + "(())" + "(()(()))".
> After removing outer parentheses of each part, this is "()()" + "()" + "()(())" = "()()()()(())".
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> s = "()()"
> ```
>
> **Output:**
>
> ```python
> ""
> ```
>
> **Explanation:**
>
> ```python
> The input string is "()()", with primitive decomposition "()" + "()".
> After removing outer parentheses of each part, this is "" + "" = "".
> ```

> [!warning]- Constraints
> - `1 <= s.length <= 10^5`
>
> 	- `s[i]` is either `'('` or `')'`.
>
> 	- `s` is a valid parentheses string.

---

### Hints:

> [!success]- Hint 1
> Can you find the primitive decomposition?  The number of ( and ) characters must be equal.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `4 ms` | `19.4 MB` | `O(n)` | `O(1)` |
>
> ```python
> class Solution:
>     def removeOuterParentheses(self, s: str) -> str:
>         myStack = []
>         sLen = len(s)
>
>         ans = ""
>         depth = 0
>         for x in s:
>             if not myStack:
>                 myStack.append(x)
>                 depth = 0
>                 continue
>
>             if (x == ")"):
>                 myStack.pop()
>                 if depth > 0:
>                     ans += x
>                 depth -= 1
>             else:
>                 myStack.append(x)
>                 ans += x
>                 depth += 1
>         return ans
>
>
>
>
>
> ```

> [!success]- Reasoning
> 1. Initialize data structures
> 2. Process input according to algorithm
> 3. Return computed result
>
> **Key insight:** Single pass through input solves problem

---

### Notes:

*(fill in)*
