---
link: https://leetcode.com/problems/smallest-divisible-digit-product-ii/description/
difficulty: Hard
date: 2026-08-07
tags: [math, string, backtracking, greedy, number-theory]
runtime: "292 ms"
memory: "33.9 MB"
---

### Problem Statement:

You are given a string `num` which represents a **positive** integer, and an integer `t`.

A number is called **zero-free** if _none_ of its digits are 0.

Return a string representing the **smallest** **zero-free** number greater than or equal to `num` such that the **product of its digits** is divisible by `t`. If no such number exists, return `"-1"`.

> [!example]- Example 1
> **Input:** num = "1234", t = 256
>
> **Output:** "1488"
>
> **Explanation:**
>
> The smallest zero-free number that is greater than 1234 and has the product of its digits divisible by 256 is 1488, with the product of its digits equal to 256.

> [!example]- Example 2
> **Input:** num = "12355", t = 50
>
> **Output:** "12355"
>
> **Explanation:**
>
> 12355 is already zero-free and has the product of its digits divisible by 50, with the product of its digits equal to 150.

> [!example]- Example 3
> **Input:** num = "11111", t = 26
>
> **Output:** "-1"
>
> **Explanation:**
>
> No number greater than 11111 has the product of its digits divisible by 26.

> [!warning]- Constraints
>
> - `2 <= num.length <= 2 * 10^5`
>   - `num` consists only of digits in the range `['0', '9']`.
>   - `num` does not contain leading zeros.
>   - `1 <= t <= 10^14`

---

### Hints:

> [!success]- Hint 1
> `t` should only have 2, 3, 5 and 7 as prime factors.

> [!success]- Hint 2
> Find the shortest suffix that must be changed.

> [!success]- Hint 3
> Try to form the string greedily.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `292 ms` | `33.9 MB` | `O(n + log t)` | `O(n)` |
>
> ```python
> class Solution:
>     def smallestNumber(self, num: str, t: int) -> str:
>         temp = t
>         for i in range(2, 10):
>             while temp % i == 0:
>                 temp //= i
>
>         if temp > 1:
>             return "-1"
>
>         n = len(num)
>         rem = [0] * (n + 1)
>         rem[0] = t
>         pos = n - 1
>
>         num_list = list(num)
>         for i in range(n):
>             if num_list[i] == "0":
>                 pos = i
>                 break
>             rem[i + 1] = rem[i] // math.gcd(rem[i], int(num_list[i]))
>
>         if rem[n] == 1:
>             return num
>
>         for i in range(pos, -1, -1):
>             while True:
>                 num_list[i] = chr(ord(num_list[i]) + 1)
>                 if num_list[i] > "9":
>                     break
>
>                 t_now = rem[i] // math.gcd(rem[i], int(num_list[i]))
>                 k = 9
>
>                 for j in range(n - 1, i, -1):
>                     while t_now % k != 0:
>                         k -= 1
>                     t_now //= k
>                     num_list[j] = str(k)
>
>                 if t_now == 1:
>                     return "".join(num_list)
>
>         ans = []
>         original_t = t
>         for i in range(9, 1, -1):
>             while original_t % i == 0:
>                 ans.append(str(i))
>                 original_t //= i
>
>         ans_str = "".join(ans)
>         padding = max(n + 1 - len(ans_str), 0)
>         ans_str += "1" * padding
>
>         return ans_str[::-1]
> ```

> [!success]- Reasoning
> 1. Validate that `t` has only prime factors 2, 3, 5, 7; if not, return "-1".
> 2. Use a forward pass with GCD to compute remainders: for each digit position, track what divisor of `t` still needs to be satisfied.
> 3. If the full number already satisfies the product divisibility, return it unchanged.
> 4. Otherwise, use backtracking from the rightmost changeable digit: increment each digit and greedily fill the rest with the largest possible digits (9 down to 2) that maintain divisibility.
> 5. If no valid suffix exists, construct the minimal zero-free number from the prime factors of `t`.
>
> **Key insight:** GCD optimization lets you track "remainder divisibility" efficiently; greedy filling from 9 ensures the smallest result.

---

### Notes:

I did not solve this, this question was too difficult for me.
