---
link: https://leetcode.com/problems/longest-subsequence-with-non-zero-bitwise-xor/description/
difficulty: Medium
date: 2026-08-15
tags: [array, bit-manipulation]
runtime: "23 ms"
memory: "33.2 MB"
---

### Problem Statement:

You are given an integer array `nums`.

Return the length of the **longest subsequence** in `nums` whose bitwise **XOR** is **non-zero**. If no such **subsequence** exists, return 0.

> [!example]- Example 1
> **Input:** nums = [1,2,3]
>
> **Output:** 2
>
> **Explanation:**
>
> One longest subsequence is `[2, 3]`. The bitwise XOR is computed as `2 XOR 3 = 1`, which is non-zero.

> [!example]- Example 2
> **Input:** nums = [2,3,4]
>
> **Output:** 3
>
> **Explanation:**
>
> The longest subsequence is `[2, 3, 4]`. The bitwise XOR is computed as `2 XOR 3 XOR 4 = 5`, which is non-zero.

> [!warning]- Constraints
> - `1 <= nums.length <= 10^5`
>
> 	- `0 <= nums[i] <= 10^9`

---

### Hints:

> [!success]- Hint 1
> What happens if you take the entire array?

> [!success]- Hint 2
> If the XOR of the entire array is 0, can removing one element help?

> [!success]- Hint 3
> What if all elements are 0?

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `23 ms` | `33.2 MB` | `O(n)` | `O(1)` |
>
> ```python
> class Solution:
>     def longestSubsequence(self, nums: List[int]) -> int:
>         total = 0
>         non_zero = False
>
>         for n in nums:
>             non_zero |= n > 0
>             total ^= n
>
>         return non_zero * (len(nums) - (not total))
> ```

> [!success]- Reasoning
> 1. Compute the XOR of all elements; this tells us if a non-zero XOR is even possible.
> 2. Track whether any element is non-zero (if all are 0, XOR can never be non-zero).
> 3. If total XOR is non-zero, return the full array length (all elements give non-zero XOR).
> 4. If total XOR is zero and there's at least one non-zero element, remove one element to break the zero state.
> 5. If all elements are zero, return 0 (no subsequence can have non-zero XOR).
>
> **Key insight:** XOR of whole array is zero ⟹ removing any one element breaks it; only all-zeros is impossible.

---

### Notes:

*(fill in)*
