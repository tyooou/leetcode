---
link: https://leetcode.com/problems/sum-game/description/
difficulty: Medium
date: 2026-08-23
tags: [math, string, greedy, game-theory]
runtime: "62 ms"
memory: "19.8 MB"
---

### Problem Statement:

Alice and Bob take turns playing a game, with **Alice**** starting first**.

You are given a string `num` of **even length** consisting of digits and `'?'` characters. On each turn, a player will do the following if there is still at least one `'?'` in `num`:

	- Choose an index `i` where `num[i] == '?'`.

	- Replace `num[i]` with any digit between `'0'` and `'9'`.

The game ends when there are no more `'?'` characters in `num`.

For Bob to win, the sum of the digits in the first half of `num` must be **equal** to the sum of the digits in the second half. For Alice to win, the sums must **not be equal**.

	- For example, if the game ended with `num = "243801"`, then Bob wins because `2+4+3 = 8+0+1`. If the game ended with `num = "243803"`, then Alice wins because `2+4+3 != 8+0+3`.

Assuming Alice and Bob play **optimally**, return `true` _if Alice will win and _`false` _if Bob will win_.

> [!example]- Example 1
> **Input:**
>
> ```python
> num = "5023"
> ```
>
> **Output:**
>
> ```python
> false
> ```
>
> **Explanation:**
>
> ```python
> There are no moves to be made.
> The sum of the first half is equal to the sum of the second half: 5 + 0 = 2 + 3.
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> num = "25??"
> ```
>
> **Output:**
>
> ```python
> true
> ```
>
> **Explanation:**
>
> ```python
> Alice can replace one of the '?'s with '9' and it will be impossible for Bob to make the sums equal.
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> num = "?3295???"
> ```
>
> **Output:**
>
> ```python
> false
> ```
>
> **Explanation:**
>
> ```python
> It can be proven that Bob will always win. One possible outcome is:
> - Alice replaces the first '?' with '9'. num = "93295???".
> - Bob replaces one of the '?' in the right half with '9'. num = "932959??".
> - Alice replaces one of the '?' in the right half with '2'. num = "9329592?".
> - Bob replaces the last '?' in the right half with '7'. num = "93295927".
> Bob wins because 9 + 3 + 2 + 9 = 5 + 9 + 2 + 7.
> ```

> [!warning]- Constraints
> - `2 <= num.length <= 10^5`
>
> 	- `num.length` is **even**.
>
> 	- `num` consists of only digits and `'?'`.

---

### Hints:

> [!success]- Hint 1
> Bob can always make the total sum of both sides equal in mod 9.

> [!success]- Hint 2
> Why does the difference between the number of question marks on the left and right side matter?

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `62 ms` | `19.8 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def sumGame(self, num: str) -> bool:
>         n = len(num)
>         gsum, qmark = 0, 0
>
>         for i in range(n // 2):
>             curr_a, curr_b = num[0 + i], num[n - 1 - i]
>             if curr_a == "?":
>                 qmark -= 1
>             else:
>                 gsum -= int(curr_a)
>
>             if curr_b == "?":
>                 qmark += 1
>             else:
>                 gsum += int(curr_b)
>
>
>         if gsum == 0: # diff is zero
>             return abs(qmark) > 0
>
>         if (gsum ^ qmark) >= 0: # has diff and side with diff has more qmarks
>             return True
>
>         diff, moves = abs(gsum), abs(qmark)
>         if moves % 2 == 0: # bob controls
>             control_area = (moves // 2) * 9
>             return control_area > diff or not (diff >= control_area - 9 and diff <= control_area)
>
>         return True # alice controls
>
>
>
>
>
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
