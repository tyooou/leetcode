---
link: https://leetcode.com/problems/maximum-number-of-balloons/description/
difficulty: Easy
date: 2026-06-22
tags: [hash-table, string, counting]
---

### Problem Statement:

Given a string `text`, you want to use the characters of `text` to form as many instances of the word **"balloon"** as possible.

You can use each character in `text` **at most once**. Return the maximum number of instances that can be formed.

> [!example]- Example 1
> **Input:**
>
> ```python
> text = "nlaebolko"
> ```
>
> **Output:**
>
> ```python
> 1
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> text = "loonbalxballpoon"
> ```
>
> **Output:**
>
> ```python
> 2
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> text = "leetcode"
> ```
>
> **Output:**
>
> ```python
> 0
> ```

> [!warning]- Constraints
> - `1 <= text.length <= 10^4`
>
> - `text` consists of lower case English letters only.
>
>
>
> **Note:** This question is the same as  2287: Rearrange Characters to Make Target String.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | — | — | `O(n)` | `O(1)` |
>
> ```python
> class Solution:
>     def maxNumberOfBalloons(self, text: str) -> int:
>         balloon_dict = defaultdict(int)
>
>         for c in text:
>             if c in "balon":
>                 balloon_dict[c] += 1
>
>         if len(balloon_dict) < 5:
>             return 0
>
>         minSets = float(inf)
>         for k, v in balloon_dict.items():
>             minSets = min(v // 2, minSets) if k in "lo" else min(v, minSets)
>         return minSets
> ```

> [!success]- Reasoning
> 1. Count only the letters that appear in "balloon" using a dict.
> 2. If fewer than 5 distinct needed letters are present, return 0 immediately.
> 3. For each letter, halve the count for `'l'` and `'o'` (each needed twice per word) and take the min across all needed letters — that's the bottleneck.
>
> **Key insight:** `'l'` and `'o'` each appear twice in "balloon", so their available counts must be halved before taking the limiting minimum.

---

### Notes:

*(fill in)*
