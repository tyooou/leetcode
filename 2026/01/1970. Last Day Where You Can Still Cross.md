---
link: https://leetcode.com/problems/last-day-where-you-can-still-cross/description/
difficulty: Hard
date: 2026-01-01
tags: [array, binary-search, depth-first-search, breadth-first-search, union-find, matrix]
runtime: "1071 ms"
memory: "25.4 MB"
---

### Problem Statement:

There is a **1-based** binary matrix where `0` represents land and `1` represents water. You are given integers `row` and `col` representing the number of rows and columns in the matrix, respectively.

Initially on day `0`, the **entire** matrix is **land**. However, each day a new cell becomes flooded with **water**. You are given a **1-based** 2D array `cells`, where `cells[i] = [r_i, c_i]` represents that on the `i^th` day, the cell on the `r_i^th` row and `c_i^th` column (**1-based** coordinates) will be covered with **water** (i.e., changed to `1`).

You want to find the **last** day that it is possible to walk from the **top** to the **bottom** by only walking on land cells. You can start from **any** cell in the top row and end at **any** cell in the bottom row. You can only travel in the** four** cardinal directions (left, right, up, and down).

Return _the **last** day where it is possible to walk from the **top** to the **bottom** by only walking on land cells_.

> [!example]- Example 1
> **Input:**
>
> ```python
> row = 2, col = 2, cells = [[1,1],[2,1],[1,2],[2,2]]
> ```
>
> **Output:**
>
> ```python
> 2
> ```
>
> **Explanation:**
>
> ```python
> The above image depicts how the matrix changes each day starting from day 0.
> The last day where it is possible to cross from top to bottom is on day 2.
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> row = 2, col = 2, cells = [[1,1],[1,2],[2,1],[2,2]]
> ```
>
> **Output:**
>
> ```python
> 1
> ```
>
> **Explanation:**
>
> ```python
> The above image depicts how the matrix changes each day starting from day 0.
> The last day where it is possible to cross from top to bottom is on day 1.
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> row = 3, col = 3, cells = [[1,2],[2,1],[3,3],[2,2],[1,1],[1,3],[2,3],[3,2],[3,1]]
> ```
>
> **Output:**
>
> ```python
> 3
> ```
>
> **Explanation:**
>
> ```python
> The above image depicts how the matrix changes each day starting from day 0.
> The last day where it is possible to cross from top to bottom is on day 3.
> ```

> [!warning]- Constraints
> - `2 <= row, col <= 2 * 10^4`
>
> 	- `4 <= row * col <= 2 * 10^4`
>
> 	- `cells.length == row * col`
>
> 	- `1 <= r_i <= row`
>
> 	- `1 <= c_i <= col`
>
> 	- All the values of `cells` are **unique**.

---

### Hints:

> [!success]- Hint 1
> What graph algorithm allows us to find whether a path exists?

> [!success]- Hint 2
> Can we use binary search to help us solve the problem?

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `1071 ms` | `25.4 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def latestDayToCross(self, row: int, col: int, cells: List[List[int]]) -> int:
>
>         def canCross(day):
>             grid = [[0] * col for _ in range(row)] # generate empty grid
>
>             for i in range(day):
>                 r, c = cells[i][0] - 1, cells[i][1] - 1 # get coords of tile
>                 grid[r][c] = 1 # set as flooded
>
>             queue = deque()
>             visited = [[False] * col for _ in range(row)]
>
>             for c in range(col):
>                 if grid[0][c] == 0: # if not flooded
>                     queue.append((0, c))
>                     visited[0][c] = True
>
>             directions = [(1,0), (-1,0), (0,1), (0,-1)]
>
>             while queue:
>                 r, c = queue.popleft()
>
>                 if r == row - 1: # at end of grid
>                     return True
>
>                 for dr, dc in directions: # check all directions and add them to queue
>                     nr, nc = r + dr, c + dc
>
>                     if 0 <= nr < row and 0 <= nc < col and \
>                        not visited[nr][nc] and grid[nr][nc] == 0:
>                         visited[nr][nc] = True
>                         queue.append((nr, nc))
>
>             return False
>
>         left, right = 1, len(cells)
>         answer = 0
>
>         while left <= right: # binary search days can cross
>             mid = (left + right) // 2
>
>             if canCross(mid):
>                 answer = mid
>                 left = mid + 1
>             else:
>                 right = mid - 1
>
>         return answer
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
