---
link: https://leetcode.com/problems/valid-mountain-array/description/
difficulty: Easy
date: 2026-01-22
tags: [array]
runtime: "154 ms"
memory: "20.6 MB"
---

### Problem Statement:

Given an array of integers `arr`, return _`true` if and only if it is a valid mountain array_.

Recall that arr is a mountain array if and only if:

	- `arr.length >= 3`

	- There exists some `i` with `0 < i < arr.length - 1` such that:

		`arr[0] < arr[1] < ... < arr[i - 1] < arr[i] `

		- `arr[i] > arr[i + 1] > ... > arr[arr.length - 1]`

> [!example]- Example 1
> **Input:**
>
> ```python
> arr = [2,1]
> ```
>
> **Output:**
>
> ```python
> false
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> arr = [3,5,5]
> ```
>
> **Output:**
>
> ```python
> false
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> arr = [0,3,2,1]
> ```
>
> **Output:**
>
> ```python
> true
> ```

> [!warning]- Constraints
> - `1 <= arr.length <= 10^4`
>
> 	- `0 <= arr[i] <= 10^4`

---

### Hints:

> [!success]- Hint 1
> It's very easy to keep track of a monotonically increasing or decreasing ordering of elements. You just need to be able to determine the start of the valley in the mountain and from that point onwards, it should be a valley i.e. no mini-hills after that. Use this information in regards to the values in the array and you will be able to come up with a straightforward solution.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `154 ms` | `20.6 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def validMountainArray(self, arr: List[int]) -> bool:
>         if len(arr) < 3:
>             return False
>
>         prev = arr[1]
>         pprev = arr[0]
>         peak = False
>
>         for a in arr[2:]:
>             if (pprev < prev and prev > a):
>                 if peak:
>                     return False
>                 else:
>                     peak = True
>             elif (pprev > prev and prev < a)  or (a == prev):
>                 return False
>
>             pprev = prev
>             prev = a
>
>         return peak
>
>
>
>
>
>
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
