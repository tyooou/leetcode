---
link: https://leetcode.com/problems/count-nodes-equal-to-average-of-subtree/description/
difficulty: Medium
date: 2026-09-10
tags: [tree, depth-first-search, binary-tree]
runtime: "53 ms"
memory: "19.6 MB"
---

### Problem Statement:

Given the `root` of a binary tree, return _the number of nodes where the value of the node is equal to the **average** of the values in its **subtree**_.

**Note:**

	- The **average** of `n` elements is the **sum** of the `n` elements divided by `n` and **rounded down** to the nearest integer.

	- A **subtree** of `root` is a tree consisting of `root` and all of its descendants.

> [!example]- Example 1
> **Input:**
>
> ```python
> root = [4,8,5,0,1,null,6]
> ```
>
> **Output:**
>
> ```python
> 5
> ```
>
> **Explanation:**
>
> ```python
> For the node with value 4: The average of its subtree is (4 + 8 + 5 + 0 + 1 + 6) / 6 = 24 / 6 = 4.
> For the node with value 5: The average of its subtree is (5 + 6) / 2 = 11 / 2 = 5.
> For the node with value 0: The average of its subtree is 0 / 1 = 0.
> For the node with value 1: The average of its subtree is 1 / 1 = 1.
> For the node with value 6: The average of its subtree is 6 / 1 = 6.
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> root = [1]
> ```
>
> **Output:**
>
> ```python
> 1
> ```
>
> **Explanation:**
>
> ```python
> For the node with value 1: The average of its subtree is 1 / 1 = 1.
> ```

> [!warning]- Constraints
> - The number of nodes in the tree is in the range `[1, 1000]`.
>
> 	- `0 <= Node.val <= 1000`

---

### Hints:

> [!success]- Hint 1
> What information do we need to calculate the average? We need the sum of the values and the number of values.

> [!success]- Hint 2
> Create a recursive function that returns the size of a node’s subtree, and the sum of the values of its subtree.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `53 ms` | `19.6 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> # Definition for a binary tree node.
> # class TreeNode:
> #     def __init__(self, val=0, left=None, right=None):
> #         self.val = val
> #         self.left = left
> #         self.right = right
> class Solution:
>     def averageOfSubtree(self, root: TreeNode) -> int:
>         def bfs(s, n, ans):
>             if not s:
>                 return 0, 0, 0
>
>             l_tot, l_n, l_ans = bfs(s.left, n, ans)
>             r_tot, r_n, r_ans = bfs(s.right, n, ans)
>
>             c_sum = s.val + l_tot + r_tot
>             c_n = 1 + l_n + r_n
>             c_ans = l_ans + r_ans
>
>             if c_sum // c_n == s.val:
>                 c_ans += 1
>
>             return c_sum, c_n, c_ans
>
>         return bfs(root, 0, 0)[2]
>
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
