---
link: https://leetcode.com/problems/image-overlap/description/
difficulty: Medium
date: 2026-09-14
tags: [array, matrix]
runtime: "146 ms"
memory: "19.5 MB"
---

### Problem Statement:

You are given two images, `img1` and `img2`, represented as binary, square matrices of size `n x n`. A binary matrix has only `0`s and `1`s as values.

We **translate** one image however we choose by sliding all the `1` bits left, right, up, and/or down any number of units. We then place it on top of the other image. We can then calculate the **overlap** by counting the number of positions that have a `1` in **both** images.

Note also that a translation does **not** include any kind of rotation. Any `1` bits that are translated outside of the matrix borders are erased.

Return _the largest possible overlap_.

> [!example]- Example 1
> **Input:**
>
> ```python
> img1 = [[1,1,0],[0,1,0],[0,1,0]], img2 = [[0,0,0],[0,1,1],[0,0,1]]
> ```
>
> **Output:**
>
> ```python
> 3
> ```
>
> **Explanation:**
>
> ```python
> We translate img1 to right by 1 unit and down by 1 unit.
>
> The number of positions that have a 1 in both images is 3 (shown in red).
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> img1 = [[1]], img2 = [[1]]
> ```
>
> **Output:**
>
> ```python
> 1
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> img1 = [[0]], img2 = [[0]]
> ```
>
> **Output:**
>
> ```python
> 0
> ```

> [!warning]- Constraints
> - `n == img1.length == img1[i].length`
>
> 	- `n == img2.length == img2[i].length`
>
> 	- `1 <= n <= 30`
>
> 	- `img1[i][j]` is either `0` or `1`.
>
> 	- `img2[i][j]` is either `0` or `1`.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `146 ms` | `19.5 MB` | `` `O(k²)` `` | `` `O(n²)` `` |
>
> ```python
> class Solution:
>     def largestOverlap(self, img1: List[List[int]], img2: List[List[int]]) -> int:
>         n = len(img1)
>         A = [(i, j) for i in range(n) for j in range(n) if img1[i][j] == 1]
>         B = [(i, j) for i in range(n) for j in range(n) if img2[i][j] == 1]
>         cnt = [[0] * (2 * n) for _ in range(2 * n)]
>         best = 0
>         for ax, ay in A:
>             for bx, by in B:
>                 dx = bx - ax + n
>                 dy = by - ay + n
>                 cnt[dx][dy] += 1
>                 best = max(best, cnt[dx][dy])
>         return best
> ```

> [!success]- Reasoning
> 1. Extract all 1-valued positions from both images into coordinate lists A and B.
> 2. Create a 2D count array sized (2n × 2n) to map all possible translations (with offset n for negative deltas).
> 3. For each pair of 1s (one from img1, one from img2), compute the translation vector dx=bx-ax+n, dy=by-ay+n.
> 4. Increment the count at this translation position; each increment represents one overlapping cell under that translation.
> 5. Track and return the maximum count value across all translation vectors.
>
> **Key insight:** Counting translations instead of simulating shifts directly solves the overlap problem in O(k²) where k is the number of 1s.

---

### Notes:

*(fill in)*
