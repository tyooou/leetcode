Problem link: https://leetcode.com/problems/power-of-four/description/?envType=daily-question&envId=2025-08-15
Difficulty: Easy
Date: 15/08/2025
Tags: Math

## Problem Summary
Given an integer `n`, return _`true` if it is a power of four. Otherwise, return `false`_.

An integer `n` is a power of four, if there exists an integer `x` such that `n == 4x`.

### **Example 1:**
**Input:** n = 16
**Output:** true

### **Example 2:**
**Input:** n = 5
**Output:** false

### **Example 3:**
**Input:** n = 1
**Output:** true

## Submission

```python
class Solution:

	def isPowerOfFour(self, n: int) -> bool:
		count = 1
		while count <= n:
			if count == n:
				return True
			count *= 4
		
		return False
```

## Reasoning

1. We use `count` which increments in powers of 4 to compare with `n` to see if `n` is a power of 4.
2. Run loop and increment `count` in powers of 4.
3. If in loop and `count` is equal to `n`, `n` is a power of 4.
4. If still in loop, and `count` is greater than `n`, `n` cannot be a multiple of 4 and thus return `False`.

## Notes

Definitely not the most efficient way, but is very simple. 
The best way would be bit manipulation probably.

EDIT: It was bit manipulation.

## Complexity
**Time:** O(log4 n)  
**Space:** O(1)
