---
link: https://leetcode.com/problems/minimum-penalty-for-a-shop/description/
difficulty: Medium
date: 2025-12-26
tags: [string, prefix-sum]
runtime: "49 ms"
memory: "18 MB"
---

### Problem Statement:

You are given the customer visit log of a shop represented by a **0-indexed** string `customers` consisting only of characters `'N'` and `'Y'`:

	- if the `i^th` character is `'Y'`, it means that customers come at the `i^th` hour

	- whereas `'N'` indicates that no customers come at the `i^th` hour.

If the shop closes at the `j^th` hour (`0 <= j <= n`), the **penalty** is calculated as follows:

	- For every hour when the shop is open and no customers come, the penalty increases by `1`.

	- For every hour when the shop is closed and customers come, the penalty increases by `1`.

Return_ the **earliest** hour at which the shop must be closed to incur a **minimum** penalty._

**Note** that if a shop closes at the `j^th` hour, it means the shop is closed at the hour `j`.

> [!example]- Example 1
> **Input:**
>
> ```python
> customers = "YYNY"
> ```
>
> **Output:**
>
> ```python
> 2
> ```
>
> **Explanation:**
>
> ```python
> - Closing the shop at the 0^th hour incurs in 1+1+0+1 = 3 penalty.
> - Closing the shop at the 1^st hour incurs in 0+1+0+1 = 2 penalty.
> - Closing the shop at the 2^nd hour incurs in 0+0+0+1 = 1 penalty.
> - Closing the shop at the 3^rd hour incurs in 0+0+1+1 = 2 penalty.
> - Closing the shop at the 4^th hour incurs in 0+0+1+0 = 1 penalty.
> Closing the shop at 2^nd or 4^th hour gives a minimum penalty. Since 2 is earlier, the optimal closing time is 2.
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> customers = "NNNNN"
> ```
>
> **Output:**
>
> ```python
> 0
> ```
>
> **Explanation:**
>
> ```python
> It is best to close the shop at the 0^th hour as no customers arrive.
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> customers = "YYYY"
> ```
>
> **Output:**
>
> ```python
> 4
> ```
>
> **Explanation:**
>
> ```python
> It is best to close the shop at the 4^th hour as customers arrive at each hour.
> ```

> [!warning]- Constraints
> - `1 <= customers.length <= 10^5`
>
> 	- `customers` consists only of characters `'Y'` and `'N'`.

---

### Hints:

> [!success]- Hint 1
> At any index, the penalty is the sum of prefix count of ‘N’ and suffix count of ‘Y’.

> [!success]- Hint 2
> Enumerate all indices and find the minimum such value.

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `49 ms` | `18 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def bestClosingTime(self, customers: str) -> int:
>         start_penalty = customers.count("N") # if always open
>         min_penalty = start_penalty
>         closing_hour = 0
>
>         for hour, customer in enumerate(customers):
>             if customer == "Y":
>                 start_penalty -= 1
>             else:
>                 start_penalty += 1
>
>             if start_penalty < min_penalty:
>                 closing_hour = hour + 1
>                 min_penalty = start_penalty
>
>
>         return closing_hour
>
>
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
