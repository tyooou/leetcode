---
link: https://leetcode.com/problems/reverse-integer/description/
difficulty: Medium
date: 2025-04-05
tags: [math]
runtime: "33 ms"
memory: "18 MB"
---

### Problem Statement:

Given a signed 32-bit integer `x`, return `x`_ with its digits reversed_. If reversing `x` causes the value to go outside the signed 32-bit integer range `[-2^31, 2^31 - 1]`, then return `0`.

**Assume the environment does not allow you to store 64-bit integers (signed or unsigned).**

> [!example]- Example 1
> **Input:**
>
> ```python
> x = 123
> ```
>
> **Output:**
>
> ```python
> 321
> ```

> [!example]- Example 2
> **Input:**
>
> ```python
> x = -123
> ```
>
> **Output:**
>
> ```python
> -321
> ```

> [!example]- Example 3
> **Input:**
>
> ```python
> x = 120
> ```
>
> **Output:**
>
> ```python
> 21
> ```

> [!warning]- Constraints
> - `-2^31 <= x <= 2^31 - 1`

---

### Submission:

> [!example]+ Submission
> | Runtime | Memory | Time | Space |
> |---|---|---|---|
> | `33 ms` | `18 MB` | `*(fill in)*` | `*(fill in)*` |
>
> ```python
> class Solution:
>     def reverse(self, x: int) -> int:
>         myList = []
>         res = 0
>         neg = 1
>         if x < 0:
>             x = -x
>             neg = -1
>
>         while x != 0:
>             myList.append(x % 10)
>             x //= 10
>
>         lenList = len(myList)
>         for i in range(lenList):
>             res += myList[i] * pow(10, lenList - 1 - i)
>
>         return res * neg if res <= pow(2, 31) - 1 else 0
> ```

> [!success]- Reasoning
> *(fill in)*

---

### Notes:

*(fill in)*
